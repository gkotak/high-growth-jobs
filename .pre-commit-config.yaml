repos:
-   repo: local
    hooks:
    -   id: ai-review
        name: AI Code Review (OpenAI)
        entry: uv run python scripts/ai_pre_commit.py
        language: system
        pass_filenames: false
        stages: [pre-commit]
